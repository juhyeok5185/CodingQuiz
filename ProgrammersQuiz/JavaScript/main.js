const numbers = [1,2,3,4,5];
solution(numbers);

function solution(numbers) {
    var answer = [];
    for (let i = 0; i < numbers.lentgh; i++){
        console.log(numbers[i]);
    }
    return answer;
}